//
// Created by svend on 017, 17-02-2023.
//

#include "garblingScheme.h"
/*

#include <string>
#include <vector>
using namespace std;

class garblingScheme {
public:
    struct garbledCircuit;
    struct encryptionLabels;
    struct decryptionLabels;
    virtual tuple<garbledCircuit, encryptionLabels, decryptionLabels> garble(int k, vector<string> f) = 0;

    struct encryptedInput;
    struct input;
    virtual encryptedInput encode(encryptionLabels e, input x) = 0;

    struct encryptedOutput;
    virtual encryptedOutput eval(garbledCircuit F, encryptedInput X) = 0;

    struct decryptedOutput;
    virtual decryptedOutput decode(decryptionLabels d, encryptedOutput Y) = 0;
};*/
