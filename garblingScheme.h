//
// Created by svend on 017, 17-02-2023.
//

#ifndef GARBLINGGATES_GARBLINGSCHEME_H
#define GARBLINGGATES_GARBLINGSCHEME_H


/*#include <string>
#include <vector>
using namespace std;

class garblingScheme {
    public:
        virtual int garble(int k, vector<string> f) = 0;
        virtual int encode(vector<tuple<vector<uint64_t>>> e, vector<uint64_t> x) = 0;
        virtual int eval(vector<vector<vector<uint64_t>>> F, vector<vector<uint64_t>> X) = 0;
        virtual int decode(vector<tuple<vector<uint64_t>>> d, vector<tuple<vector<uint64_t>>> Y) = 0;
};*/


#endif //GARBLINGGATES_GARBLINGSCHEME_H
